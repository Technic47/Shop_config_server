spring:
  application:
    name: Config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Technic47/Shop_Configurations
          default-label: main
          search-paths: "{application}"

server:
  port: ${SERVICE_PORT}